#include <stdio.h>

/**
 * main - accepts no parameters and returns an integer
 *
 * Return: statement to return the integer value
 */

int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
		putchar(i);
	putchar("\n");
		return (0);
}
